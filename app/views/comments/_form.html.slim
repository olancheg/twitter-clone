= simple_form_for [tweet, comment], html: { class: 'comment-form' } do |f|
  = f.input :body, as: :text, label: false, placeholder: 'Please enter your comment (up to 140 symbols)'
  = f.submit 'Submit', class: 'btn btn-primary'
